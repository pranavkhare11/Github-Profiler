const FollowingPage = () => {
    return (
        <section className="panel content-panel">
            <h2 className="page-title">Following</h2>
            <p className="status-text">Following list will render here.</p>
        </section>
    );
};

export default FollowingPage;